# R / RStudio
.Rproj.user/
.Rhistory
.RData
.Ruserdata
# Quarto rendering artifacts
*_files/
*_cache/
.quarto/
# Output (regenerated)
output/*.pdf
# Triage carry-over state — derived counts by division, regenerated each run
output/*.csv
# Cached report data — derived from data/, so treat as PHI-bearing
.cache/
# Data files (contain PHI — do not commit)
data/*.xlsx
data/*.csv
# OS files
.DS_Store
Thumbs.db
.Rproj.user
.httr-oauth
.quarto
.positai
old_data/
old_output/
